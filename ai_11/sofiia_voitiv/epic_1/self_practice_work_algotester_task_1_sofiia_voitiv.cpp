#include <iostream>
using namespace std;

int main() {
    int l,w,u,d;
    cin >> l >> w >> u >> d ;

if (l > w || l > u+d ) {

    cout << "Forget about the cocktails, man!";
}
else {
    cout << "Three times Sex on the Beach, please!";
}
    return 0;
}